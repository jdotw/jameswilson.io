---
const { article } = Astro.props;
import { Image, Picture } from "@astrojs/image/components";
import { Debug } from "astro/components";
---

<article class="flex flex-col items-start justify-between bg-white w-full">
  <div class="relative w-full">
    <Image
      src={`/${article.data.image}`}
      height={500}
      width={500}
      format="webp"
      alt={article.data.title}
      class="aspect-[16/9] w-full rounded-lg bg-gray-100 object-cover sm:aspect-[2/1] lg:aspect-[3/2]"
    />
    <a href={`/articles/${article.slug}`} class="">
      <div
        class="absolute inset-0 rounded-lg ring-1 ring-inset ring-gray-900/10"
      >
      </div>
    </a>
  </div>
  <div class="max-w-xl">
    <h2 class="mt-3 heading-2">
      <a href={`/articles/${article.slug}`}>
        {article.data.title}
      </a>
    </h2>
    <div class="group relative">
      <p class="mt-5 text-1">
        {article.data.excerpt}
        <a
          href={`/articles/${article.slug}`}
          class="text-md font-normal text-gray-800">Read more</a
        >
      </p>
    </div>

    <div class="mt-5 flex items-center gap-x-4 text-xs">
      <time datetime="2020-03-16" class="text-gray-500"
        >{new Date(article.data.date).toLocaleDateString()}</time
      >
      {
        article.data.tags.map((tag: string) => (
          <a
            href={`/tags/${tag}`}
            class="relative z-10 rounded-full bg-gray-50 py-1.5 px-3 font-medium text-gray-600 hover:bg-gray-100"
          >
            {tag}
          </a>
        ))
      }
    </div>
  </div>
</article>
